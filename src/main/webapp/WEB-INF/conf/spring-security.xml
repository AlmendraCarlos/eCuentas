<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">
 
 	<beans:import resource="data-source.xml" />
 
	<http auto-config="true">
		<intercept-url pattern="/login.htm" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
		<intercept-url pattern="/resources/img/*" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
		<intercept-url pattern="/resources/jQuery/**" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
		<intercept-url pattern="/resources/bootstrap/**" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
		<intercept-url pattern="/**" access="ROLE_USER" />
		<form-login 
		    login-page="/login.htm" 
		    default-target-url="/home.htm" 
			authentication-failure-url="/login.htm?error" 
			username-parameter="username"
			password-parameter="password" />
		<logout logout-success-url="/login.htm?logout" />
		<!-- enable csrf protection -->
		<csrf/>
	</http>
 
 	<!-- Select users and user_roles from database -->
	<authentication-manager>
	  <authentication-provider>
		<jdbc-user-service data-source-ref="dataSource"
		  users-by-username-query=
		    "select nombre as username, password, activo as enabled from usuario where nombre=?"
		  authorities-by-username-query="select u.nombre as username, ur.rol as role from usuario_rol ur 
						inner join usuario u on ur.id_usuario = u.id
					   where nombre = ?"/>
	  </authentication-provider>
	</authentication-manager>
 
</beans:beans>